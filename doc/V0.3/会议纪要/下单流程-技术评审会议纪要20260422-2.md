会后需要处理支付流程调整、订单状态流转、库存校验、订单号生成、积分支付规则、物流跟踪、个性化推荐组件、订单流程确认等内容，并补充相关文档发到群里。

1. 支付流程调整
修改支付页面，确认支付按钮行为（如下单加支付弹窗或只下单），调研支付页面开发需求。
处理支付超时策略，在文档中补充支付超时时间（如前端15分钟、后端半小时关单）。

2. 订单状态流转
完善订单状态流转文档，交流后发到群里。

3. 库存校验
讨论库存数量的口径，确认库存校验规则（如指定供应商库存校验、最低价供应商库存校验）。

4. 订单号生成规则
确定订单号生成规则，问金哥建议，确认是否包含时间戳、品牌商城缩写、序列号等。

5. 积分支付规则
处理积分支付时的商品过滤规则（如指定商品、品牌、分类），积分分配规则（如按比例分配或平摊），并写清楚如何处理小数情况。

6. 物流跟踪
确认物流单号的来源和显示规则（如从供应链推过来），物流状态显示（如多个包裹取最滞后状态）。

7. 个性化推荐组件
确认买点事件（如下单、加购、收藏），处理猜你喜欢逻辑（如按1级分类推荐、热销商品）。

8. 订单流程确认
确定订单创建后的流程，是否先到OMS还是直接到供应链，并写流程图。

9. 供应商分配和库存
找穆瑞确认库存规则，指定供应商的库存校验。

10. 文档补充
将补充内容（如支付超时、订单状态流转、库存校验规则）发到群里。

11. 其他调整
改订单详情页，加状态多选等。